pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id("org.openapi.generator") version "7.12.0"
    }
}

rootProject.name = "auth-server"
